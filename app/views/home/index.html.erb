<div class="container">
<h2>Campaigns</h2>
	<ul class="list-group">
		<table class="table">
		    <thead>
		      <tr>
		        <th>Campaign Name</th>
		        <th>Total tokens to be air dropped</th>
		        <th>Token percentage for contribution</th>
		        <th>Token percentage for referrel</th>
		        <th>Please provide your details below to get tokens</th>
		      </tr>
		    </thead>
		    <tbody>
				<% @campaigns.each do |campaign| %>
					<tr>
						<td><%= campaign.name %></td>
						<td><%= campaign.tokens_distributed_for_each_referral %></td>
						<% count = 0 %>
						<% campaign.user_campaign_mappings.each do |u| %>
							<% count  = count + u.referrals %>
						<% end %>
						<td><%= count %></td>
						<td><%= count %></td>
						<td><a href="/home/campaign?id=<%= campaign.id %>"><button class="btn btn-success">Click Here</button></a> </td>
					</tr>
	    		<% end %>
	    	</tbody>
	    </table>
	</ul>
</div>